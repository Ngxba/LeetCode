# def pytest_configure(config):
#     # register an additional marker
#     config.addinivalue_line(
#         "markers", "max_consecutive_ones: mark test to run only on introduction max_consecutive_ones"
#     )
#     config.addinivalue_line(
#         "markers", "find_even_numbers: mark test to run only on introduction find_even_numbers"
#     )
